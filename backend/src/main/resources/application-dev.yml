spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/online_shop
    username: shop_user
    password: shop_secret

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.onlineshop: DEBUG